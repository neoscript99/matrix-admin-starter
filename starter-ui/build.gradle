plugins {
    id "com.moowork.node" version "1.3.1"
}
node {
    download = false
}

task uiBuild(type: NpmTask) {
    args = ['run', 'build']
}

evaluationDependsOn(":starter-ui-lib")
def libBuild = project(":starter-ui-lib").tasks.uiBuild
uiBuild.dependsOn(libBuild)
